version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-assistant-frontend
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    networks:
      - travel-network
    restart: unless-stopped

networks:
  travel-network:
    driver: bridge

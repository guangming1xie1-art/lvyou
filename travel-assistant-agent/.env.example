# Application
APP_NAME=travel-assistant-agent
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# ============ LLM Provider API Keys ============
# DeepSeek (便宜层主力 - ¥0.0014/1k tokens)
DEEPSEEK_API_KEY=
# 阿里云通义千问 (标准层主力 - ¥0.008/1k tokens)
DASHSCOPE_API_KEY=
# 智谱 AI GLM (标准层备选 - ¥0.01/1k tokens)
ZHIPU_API_KEY=
# OpenAI 兼容 (GPT-4o 系列)
OPENAI_API_KEY=
# Anthropic Claude (强力层 - $3/1M tokens)
ANTHROPIC_API_KEY=

# ============ LLM Tier Strategy (模型分层策略) ============
# 便宜层默认提供商：deepseek / qwen-plus / gpt-4o-mini
LLM_CHEAP_PROVIDER=deepseek
# 标准层默认提供商：qwen-turbo / glm-4 / gpt-4o
LLM_STANDARD_PROVIDER=qwen-turbo
# 强力层默认提供商：claude / qwen-max / deepseek-reasoner
LLM_POWER_PROVIDER=claude

# LLM 通用参数
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096

# ============ Legacy Claude API (保留兼容性) ============
CLAUDE_MODEL=claude-3-5-sonnet-20241022
CLAUDE_MAX_TOKENS=4096
CLAUDE_TEMPERATURE=0.7

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travel_assistant
DB_HOST=localhost
DB_PORT=5432
DB_NAME=travel_assistant
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Authentication
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Security
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
REQUIRE_HTTPS=false
MIN_PASSWORD_LENGTH=8
REQUIRE_SPECIAL_CHARS=true

# Backend API
BACKEND_API_URL=http://localhost:3000/api

# Redis Cache
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_MAX_CONNECTIONS=50

# Performance
ENABLE_GZIP=true
GZIP_MIN_SIZE=1000
SLOW_REQUEST_THRESHOLD=1.0
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# MCP (Model Context Protocol)
MCP_ENABLED=true
MCP_SERVER_URL=http://localhost:8765
MCP_TRANSPORT=stdio

# Java API (Backend Service)
JAVA_API_BASE_URL=http://localhost:8080/api
JAVA_API_TIMEOUT=30
JAVA_API_MAX_RETRIES=3
JAVA_API_AUTH_TOKEN=

# Gradio配置
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=7860
GRADIO_SHARE=false

# Agent配置
AGENT_URL=http://localhost:8000
AGENT_TIMEOUT=60

# 多媒体配置
MAX_IMAGE_SIZE=10485760
MAX_AUDIO_SIZE=52428800
MAX_VIDEO_SIZE=104857600

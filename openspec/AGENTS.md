# OpenSpec AI Agent Workflow Guide

> Auto-generated by OpenSpec for cto.new integration - lvyou 项目

## Overview

This file provides AI agent workflow guidance for the lvyou monorepo. All AI agents should read this file before starting work on any task.

## Project Context

**lvyou** is a smart travel assistant platform with three sub-projects:

1. **travel-assistant-front** (React 18 + TypeScript + Vite)
2. **travel-assistant** (Java Spring Cloud microservices)
3. **travel-assistant-agent** (Python FastAPI + LangGraph + Claude)

**Primary AI Tool**: cto.new
**Fallback**: Claude Code with AGENTS.md reference

## Before Starting Any Task

### Step 1: Understand the Scope

1. **Read the ticket** to understand the requirement
2. **Identify affected services** from the ticket
3. **Check relevant OpenSpec specifications**:
   - `openspec/specs/frontend/spec.md` (if frontend changes)
   - `openspec/specs/backend-java/spec.md` (if Java backend changes)
   - `openspec/specs/backend-agent/spec.md` (if Agent changes)
   - `openspec/specs/integration/spec.md` (if cross-service changes)

### Step 2: Review Existing Specifications

```bash
# List all specs
cat openspec/specs/*/spec.md

# Check project conventions
cat openspec/project.md
```

### Step 3: Check for Active Changes

```bash
# List in-progress changes
ls openspec/changes/

# Review specific change
openspec show <change-name>
```

## cto.new Specific Guidelines

### Task Execution Pattern

When executing a cto.new task:

1. **Read relevant specs first** - Understand the constraints before coding
2. **Follow naming conventions** from `openspec/project.md`
3. **Use the correct technology stack** for each service:
   - Frontend: React, TypeScript, Zustand, TanStack Query
   - Java: Spring Boot 3.x, Spring Cloud, Nacos
   - Python: FastAPI, LangGraph, Pydantic

4. **Match existing code patterns** - Look at similar implementations in the codebase

5. **Update specifications if needed** - If the task reveals spec gaps, propose changes

### Code Style Enforcement

| Service | Linter | Formatter | Configuration |
|---------|--------|-----------|---------------|
| Frontend | ESLint | Prettier | `.eslintrc.js`, `.prettierrc` |
| Java | - | - | Maven standard |
| Python | Ruff | Black | `pyproject.toml` |

### API Development

When creating or modifying APIs:

1. **Check existing API contracts** in `openspec/specs/integration/spec.md`
2. **Follow RESTful conventions** (GET/POST/PUT/PATCH/DELETE)
3. **Use standard response format**:
   ```json
   {
     "code": 0,
     "message": "OK",
     "data": {},
     "timestamp": "2025-01-01T00:00:00Z"
   }
   ```
4. **Version APIs** in URL path: `/api/v1/...`
5. **Document changes** in the relevant spec file

### Database Changes

For database schema changes:

1. **Check existing schema** in `openspec/specs/backend-java/spec.md`
2. **Follow naming conventions**:
   - Tables: `snake_case` plural (`travel_requests`)
   - Columns: `snake_case` (`created_at`)
   - Primary key: `id` UUID
3. **Add migration documentation** to the relevant spec
4. **Update related API contracts** if schema affects responses

## Change Documentation Pattern

### For New Features

1. **Create change folder** (if not exists):
   ```bash
   mkdir -p openspec/changes/{feature-name}
   ```

2. **Create proposal.md**:
   ```markdown
   # Feature Proposal: {feature-name}

   ## Summary
   Brief description of the feature

   ## Motivation
   Why this feature is needed

   ## Requirements
   - List specific requirements
   ```

3. **Create tasks.md**:
   ```markdown
   # Implementation Tasks

   ## Frontend
   - [ ] Task 1
   - [ ] Task 2

   ## Backend
   - [ ] Task 3
   ```

4. **Create design.md**:
   ```markdown
   # Technical Design

   ## Architecture
   ## Data Flow
   ## API Contracts
   ```

5. **Update specs** in `specs/` directory with new requirements

### During Implementation

- **Reference specifications** when making decisions
- **Mark tasks complete** in `tasks.md` as you progress
- **Document deviations** if specs need updating

### After Completion

1. **Validate changes**: `openspec validate`
2. **Archive change**: `openspec archive <change-name>`
3. **Commit updated specs** along with code

## Specification Validation

Before finishing any task:

```bash
# Validate all specs are consistent
openspec validate

# Show pending changes
openspec show <change-name>
```

## Common Patterns

### Adding a New API Endpoint

**Java Backend**:
1. Create Controller in `travel-assistant/{service}/src/main/java/.../controller/`
2. Define DTOs in `dto/` package
3. Update `openspec/specs/backend-java/spec.md`
4. Add tests

**Python Agent**:
1. Create/update endpoint in `src/main.py` or `src/agents/`
2. Define Pydantic models in `src/models/schemas.py`
3. Update `openspec/specs/backend-agent/spec.md`
4. Add tests

### Adding a Frontend Feature

1. Create components in `src/components/`
2. Add page in `src/pages/`
3. Update state in `src/store/` (Zustand) or use React Query
4. Create/update API service in `src/services/`
5. Update `openspec/specs/frontend/spec.md`

### Cross-Service Changes

1. Update integration spec: `openspec/specs/integration/spec.md`
2. Update all affected service specs
3. Ensure API contracts are consistent
4. Document data flow changes

## Quick Reference

### Key Files

| File | Purpose |
|------|---------|
| `openspec/project.md` | Project-wide conventions |
| `openspec/specs/frontend/spec.md` | Frontend specifications |
| `openspec/specs/backend-java/spec.md` | Java backend specifications |
| `openspec/specs/backend-agent/spec.md` | Agent specifications |
| `openspec/specs/integration/spec.md` | API contracts |
| `openspec/AGENTS.md` | This file |

### Service Ports

| Service | Port | URL |
|---------|------|-----|
| Frontend | 3000 (dev) | http://localhost:3000 |
| Gateway | 8080 | http://localhost:8080 |
| Auth Service | 8081 | - |
| Request Service | 8082 | - |
| Plan Service | 8083 | - |
| Agent | 8000 | http://localhost:8000 |

### Naming Quick Reference

| Type | Convention | Example |
|------|-----------|---------|
| React Component | PascalCase | `TravelCard.tsx` |
| Python Class | PascalCase | `TravelAgent` |
| Java Class | PascalCase | `TravelController` |
| Function/Method | snake_case | `create_travel_plan()` |
| API Path | kebab-case | `/travel-requests` |
| Table | snake_case | `travel_requests` |

## Getting Help

1. **Check specifications first** - Most questions are answered in specs
2. **Review existing code** - Similar implementations exist
3. **Check project conventions** in `openspec/project.md`
4. **Review related changes** in `openspec/changes/`

## Error Handling

If you encounter issues:

1. **Validate specs**: `openspec validate`
2. **Check for typos** in paths and names
3. **Verify dependencies** are installed
4. **Check service health** for integration issues

---

*This file is auto-generated by OpenSpec. Do not edit manually.*
